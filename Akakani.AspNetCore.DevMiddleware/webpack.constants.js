// -----------------------------------------------------------------------------------
// Copyright 2021, AKAKANI.COM
// -----------------------------------------------------------------------------------

"use strict";

const outputRoot = "Content";

module.exports = {
    // All outputs are produced under this directory
    outputRoot: outputRoot,

    // Paths where output are produced - Useful for cleaning generated files
    outputPaths: [
        outputRoot
    ]
};